{
  "name": "dark-horse-mobility-campaigns",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ imthatcarlos/dark-horse-mobility-campaigns",
    "create-local": "graph create --node http://localhost:8020/ imthatcarlos/dark-horse-mobility-campaigns",
    "remove-local": "graph remove --node http://localhost:8020/ imthatcarlos/dark-horse-mobility-campaigns",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 imthatcarlos/dark-horse-mobility-campaigns"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
